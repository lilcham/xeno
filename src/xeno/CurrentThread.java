package xeno;

public class CurrentThread extends FizzBuzzThread {
    public CurrentThread(boolean checkThree, boolean checkFive, int max, String toPrint) {
        super(checkThree, checkFive, max, toPrint);
    }
}
